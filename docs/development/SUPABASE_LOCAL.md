# Supabase local

See also `supabase/README.md`.

```bash
npx supabase start
npx supabase status
```

Map keys into `.env.local`. Stop with `npx supabase stop`.

Increment 1 has **empty migrations** — no domain schema yet.
